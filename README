This repository contains some utility bash scripts written for personal use, but if you find them useful you can download and use them..

Actually the available scripts are: 
1. convert - A simple script to convert all the audio files into a folder in mp3 format.
